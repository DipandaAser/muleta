{
  "name": "@muleta-dev/demo",
  "version": "0.0.0",
  "description": "Public demo for muleta — populates a Redis with realistic queues, jobs, schedulers, and flows so visitors can click through a live dashboard.",
  "license": "Apache-2.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@faker-js/faker": "^10.4.0",
    "bullmq": "^5.74.1",
    "ioredis": "^5.10.1"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=22"
  }
}
